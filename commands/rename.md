---
description: Rename a page and fix every link
---

Rename as specified in $ARGUMENTS. File, frontmatter, alias, every inbound link, log entry, then verify.

Follow the `second-brain-rename` skill.
