---
description: Find the path between two ideas
---

Find how the two things in $ARGUMENTS connect in the vault. Show the path through pages, not just the endpoints.

Follow the `second-brain-query` skill.
