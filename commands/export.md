---
description: Export a page or set of pages
---

Export $ARGUMENTS to `output/` in the requested format, rewriting links to unpublished pages as plain text.

Follow the `second-brain-publish` skill.
