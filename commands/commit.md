---
description: Commit the current state with a useful message
---

Stage the vault, write a commit message naming what changed and which run produced it, and report the diff summary.

Follow the `second-brain-lint` skill.
