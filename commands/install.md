---
description: Install the skills, commands and agents
---

Copy skills, commands and agents into `.claude/` inside the vault so they are versioned with the notes, and report what was installed.

Follow the `second-brain-lint` skill.
